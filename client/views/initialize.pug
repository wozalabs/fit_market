extends index.pug

block content
    h2 Initialize a new Product or Pallet

    p
        | Refresh page to get new Credentials

    pre
        code(class='language-json')= JSON.stringify(accountCredentials, null, 2)
